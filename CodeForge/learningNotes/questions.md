what happens if claims are changed in the token if role is changed from user to admin -> the signature of the token depends on all 3 header payload and secret key , so if anything in the token is changed the secretkey changes and it wont match , parser of jwt method takes the input payload and generates signature again and compares it with the original signature sent by the token so changes in payload are caught